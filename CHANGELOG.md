# Changelog

本项目版本号采用 `v主版本.次版本.修订版本` 格式。推送代码时，CI 会构建带日期和当日序号的版本并将 APK 保存在 Action 成品中；Release 工作流手动输入版本号后创建正式 Release，并在说明中列出 commit 更新内容。

## 未发布

### 修复

- **重定向外部链接把页面拦成空白页**（#33）：内置网页容器名单是**精确类名**，而「赛事中心」这类
  小程序页面的宿主 `MiniProgramHostActivity` 是 `WebActionActivity` 的**子类**
  （同族还有 `InjectJsV2Activity`）：入口拦截认不出它，`finish()` 判定也漏掉它。
  于是页面被全局 `WebView.loadUrl` 拦截吞掉（`return null`）、容器又留在返回栈里 ——
  浏览器打开了，回到小黑盒只剩空白页。现在改成：
  - **容器按继承链判定**：新增容器家族根类（`WebActionActivity` / `NativeWebActionActivity`），
    自身或任一父类命中即算容器，宿主新增子类自动覆盖；`redirectLoadedPage()` 的 `finish()`
    与入口 `onCreate` 兜底都改用它（后者原先挂在 `BaseActivity` 上，任何带 `pageurl` 的
    Activity 都可能被结束，现在只对容器家族生效）
  - **关不掉的容器一律不拦**：`loadUrl` 命中规则时先判容器能否安全关闭
    （容器家族 + 未在结束流程中），关不掉就只记一条 `跳过重定向(容器不可关闭)` 日志、
    放行内置加载，绝不会出现「浏览器打开了、App 里页面是空的」
  - **入口层同一口径**：`execStartActivity` 的容器判定也改成家族继承链（解析不到 Class
    才回落精确名单），地址优先取 `pageurl`、没有则从 `web_protocol.webview.url` 取
    （小程序等协议容器的地址不在 `pageurl` 上，之前这一层对它们等于没挂）。命中规则时在容器
    创建前就把 Intent 改写成 `ACTION_VIEW`，不再有「先开内置页再跳走」的闪跳；小程序页面同样留内置
- **官方 H5 / 小程序页面强制内置**（#33 收尾，方案 C）：官方网页与小程序页面都在
  `web.xiaoheihe.cn`（debug 包 `web.debugmode.cn`）下，登录态靠内置 WebView 注入 Cookie。
  现在判据里加了一道官方页规则，位于「强制重定向域名」之后：命中即留内置，不再受
  「包含小黑盒域名」影响（用户显式写进「强制重定向域名」时仍可覆盖）。`loadUrl` 层另加
  小程序宿主判定（类层级含 `miniprogram` / `littleprogram` 包），小程序首屏一律留内置，
  页面里点出去的外链仍照常由页内跳转拦截走外部浏览器

- **关键词屏蔽在社区 / 话题页不生效**（#28）：社区 Tab、话题详情、频道、游戏评测等列表走的是
  `module.bbs.adapter.t` 这条渲染链，模块此前只挂了首页流与概念信息流，这条链**从来没有挂点**，
  所以关键词 / 低等级 / 视频帖判据在这类页面全部失效。现在补了两层：
  - **数据层**：挂宿主公共出口 `BBSLinkListResultObj#getLinks()`（社区 / 话题 / 频道 / 榜单 /
    搜索 / 资讯等约 30 处调用共用），命中即从列表里丢弃，不留空白格；
    只处理「有作者」的帖子条目，菜单卡 / 横幅 / 登录提示不参与判据；
    收藏夹、草稿箱、收藏搜索页按调用栈排除，不会吞掉你自己的数据
  - **渲染层**：按方法签名 `(ViewHolder, BBSLinkObj) → void` 挂 8 个列表适配器
    （`module.bbs.adapter.t`、榜单、用户主页、搜索 ×3、资讯 ×2），兜住数据层之外的复用与二次绑定
- **动态推送点开跳到了浏览器**：通知栏与横幅里的 `ACTION_VIEW` 由**系统**解析，
  装了浏览器就会落到浏览器（没有登录态，也看不到盒内的评论区）。现在改为显式指向宿主自己的组件：
  先让 `RouterActivity` 按 web/share 链接分发，其次直开帖子详情页
  `NormalPostPageActivity` 并带上 `link_id`，两个组件都找不到才回退浏览器。
  横幅点击原先同样走浏览器，与通知栏一并统一
- **首页推荐流的关键词屏蔽此前从未命中**：首页流的条目是 `LinkFeedsFlowItemModel`，
  它没有 `getTitle()` / `getText()`（标题与摘要挂在 `getLinkContent()` 上），
  旧的 `keywordHit()` 恒返回 null —— 等级 / 视频 / 推广 / AI 判定正常，唯独关键词无效。
  现在判据依次回退到卡片摘要 `getDescription()` 与 `getLinkContent()` 的标题/摘要

- **液态玻璃底栏「自适应」宽度等于占满、底栏横贯全屏**（#34）：自适应模式按 96dp/标签
  的**上限**估宽，5 个标签要 584dp，在 360-430dp 宽的手机上必然被夹到父宽度 ——
  自适应与占满渲染完全一致；`applyBarSideMargins()` 还会把 host 上预先加好的
  10dp 侧边距写成 0，于是玻璃条贴住左右屏幕边缘（issue 截图像素扫描：条边缘距屏幕 ≈1.5dp）。
  现在：
  - **自适应按内容取宽**：实测最宽标签的文字（`Paint.measureText`）/图标固有宽 + 18dp 呼吸余量，
    下限 52dp、上限仍是 96dp × Tab 宽度缩放；中缝按加号浮钮的实测宽度计（兜底 64dp），不再按 96dp
  - **左右留白成为三种模式的公共下限**：占满只占「可用宽度」，自适应与自定义都在留白内居中，
    玻璃条任何时候都不再贴边；自适应因此落在「内容宽 ~ 可用宽度」之间，既不满屏也不挤标签
  - **新增「左右边距」滑杆**（0-40dp，默认 16dp），自定义宽度改为占可用宽度的 40-100%
  - 宽度模式 / 左右边距 / Tab 宽度纳入配置备份，「恢复默认」一并复位
- **模块面板里的「玻璃宽度自适应」开关拨了没反应**（#34）：这一行实际绑的是
  「加长选中 Tab」（`fitTabs`），只在**隐藏了标签**时才生效，5 个标签全显示时拨动等于没拨；
  且面板只写 pref、不重载 `GlassConfig`，玻璃静态字段要等小黑盒重启才更新。现在该行
  更名对齐为「加长选中 Tab」，新增「玻璃条宽度」一行直接打开调节面板，玻璃相关开关与
  数值改完即时重排（无需重启）

### 新增

- **评论区自由复制**（#32，默认开启）：长按评论的菜单保持不变，点「复制」后多出一个二级菜单：
  `复制全部内容`（原来的整条复制）、`自由复制`、`复制 @昵称`、`取消`。
  「自由复制」会打开一个居中卡片，**完整**显示这条评论的原文（长评论未展开也照样给全文、保留 `[表情]` 标记），
  **长按即可唤起系统文本选择**自由框选复制（拖动连续跟手，选择时不会误触滚动）；
  也支持模块**自绘手柄**（跟随「解除复制 → 自绘制文本选择」开关，与帖子正文同一套）；
  卡片高度随内容自适应（最多约 55% 屏高后内部滚动），
  并与手势导航栏联动（卡片下方的导航栏同样是页面底色）。宿主那句「已复制到剪贴板」的提示会被抑制，
  只有真正复制时才提示。开关在「解除复制」分组，评论区其它行为一律不变。

- **动态推送**（#26）：关注的作者发布新动态、或监控关键词命中的新帖会在小黑盒内提醒
  - **关注对象**：一行一个，支持 userid 或用户主页链接（最多 30 个）
  - **监控关键词**：一行一个，命中标题或正文即提醒，`regex:` 前缀支持正则（与发帖过滤同一套写法）
  - **三种提醒方式**：应用内横幅（自绘，挂在当前界面顶部，点击直达帖子）、
    系统通知（点击跳转帖子）、第三方推送（钉钉机器人 / WxPusher / **AstrBot**（aiocqhttp，OneBot v11）/ 自定义 webhook）
  - **检查时机**：打开小黑盒、宿主收到任意推送（搭便车唤醒，无需任何凭据）、信息流命中；
    引擎内自带最小间隔节流（默认 10 分钟）
  - **取数复用宿主自身的 OkHttp**：捕获宿主的 `OkHttpClient` 实例后发出自己的请求，
    签名（hkey/_time/nonce）、Cookie、UA 全部由宿主拦截器完成 —— 不自己实现签名，也就不随算法变更失效；
    端点为 `bbs/app/profile/user/link/list`（1.3.393 dex 中确认）
  - 默认关闭；未配置监控目标时不发起任何请求
  - 「测试提醒」一键验证三种输出：横幅 ✓ · 通知 ✓ · 推送 N 个渠道
  - 「调试：推送最近 3 条」：拉取关注对象最近 3 条帖子并按真实流程直接推送
    （忽略时间窗与去重），用于验收取数 / 横幅 / 通知 / 第三方推送整条链路
  - **跨版本**：1.3.393 / 1.3.394 / 1.3.395 三个版本对本功能用到的目标类与接口路径完全一致
    （已对 1.3.393 与 1.3.395 的 APK 逐一核对），且每个目标都按候选列表倒序尝试、
    命中情况汇总成一条日志，便于版本更新后排查
- **AstrBot 机器人接入**：「AstrBot 机器人」配置格式为 `http://主机:6199|群号|访问令牌`（后两段可省，
  也支持 `private:QQ号`），适配 AstrBot 的 aiocqhttp 适配器；设了访问令牌时以
  `Authorization: Bearer` 发送，消息按纯文本发送（`auto_escape`）避免标题里的 CQ 码被解析

- **关注话题 / 拉流 / 推荐关键词**：三个新的取数方向
  - **导入关注话题**：读取小黑盒「我关注的话题」（`bbs/app/profile/preference_v5/topic_list`，
    失败回落 `bbs/app/profile/topic/settings`），写入 `话题id|话题名`，id 可用于按话题拉流
  - **关键词 / 话题拉流**：按关键词搜索（`bbs/app/api/general/search/v1` → `bbs/app/hashtag/search`
    → `bbs/app/topic/search`）、按话题取最新帖（`bbs/app/topic/feeds` → `bbs/app/topic/max/feeds`
    → `bbs/app/hashtag/concept/feeds`）；首次见到某个关键词/话题只登记基线不推送，
    单轮最多 5 个关键词，`regex:` 关键词跳过搜索
  - **推荐关键词**：`bbs/app/api/search/hot_words` + `bbs/app/api/search/suggestion/v2`，
    弹窗里点一下即加入监控关键词（不覆盖已有配置）
  - 端点均已在 1.3.393 / 1.3.395 的 dex 中确认存在
- **设置项**：新增「获取时间窗」（30 分钟 ~ 30 天，按分钟存 `watch_window_min`，旧键 `watch_window_days` 仍兼容）、
  「检查间隔」（5 分钟 ~ 12 小时）、「关键词只匹配标题」
- **设置页 v2**：主设置页的动态推送分组精简为「总开关 + 入口 + 立即检查」，
  详细设置移入二级页「动态推送设置」，按监控目标 / 抓取范围 / 提醒方式 / 第三方推送 / 测试与调试分为 5 组

- **设置页全面归类（v2 两级结构）**：一级页只保留「功能分类」入口，全部设置项按分类移入二级页
  - 广告与内容过滤（广告过滤 / 发帖过滤 / 分享净化）、界面与外观（液态玻璃 / 底部导航栏隐藏 / 实验性功能）、
    浏览与下载（解除复制 / 网页 / 视频下载）、动态推送（总开关 / 监控目标 / 抓取范围 / 提醒方式 /
    第三方推送 / 测试与调试）、每日任务、通用与备份（通用 / 配置备份 / 关于）
  - 二级页左上角箭头与系统返回键都回到分类页；改开关后面板原地刷新仍停留在当前页
  - 修复切换页面的触摸穿透：旧面板移除后，同一次触摸的后续事件会落到新面板上
    （实测点入口行会顺带点开二级页同一位置的条目），新面板加了 300ms 触摸屏蔽窗口
  - **设置项搜索**：标题栏下方新增搜索框，输入即跨页面检索全部设置项（匹配标题与说明，
    按所属分类分组显示命中数量），点结果可直接改开关或进编辑框；面板原地刷新仍保留搜索词

- **取数接口按真机实测对齐**（用「记录宿主请求 URL」的诊断抓下宿主自己的写法对照）
  - **按话题拉流已对齐**：宿主用的是 bbs/app/topic/feeds?topic_id=<id>&offset=0&limit=30，与实现里的首选参数一致
  - **关键词搜索必须带筛选参数**：bbs/app/api/general/search/v1 不带 search_type=link&type=link 时返回空 items，现在固定带上
  - **热搜词字段是 key**：bbs/app/api/search/hot_words 的候选词在 list[].key，之前漏了这个字段名，会把广告横幅文字也当成候选词
  - **「我关注的话题」接口拿不到数据**：bbs/app/profile/preference_v5/topic_list 固定返回 msg=请至少选择一个平台，
    平台参数名没有公开信息（platform / platforms / platform_id / platform_type / platform_list 都试过），
    因此改为**回落到「最近浏览过的话题」**：模块顺手记下宿主请求里的 topic_id，
    再用 bbs/app/topic/list_infos?topic_ids= 批量换回名字 —— 没关注过话题的用户同样可用
  - 新增**话题搜索**：bbs/app/api/search/topic → topic/search → hashtag/search，搜到后点一下即加入「关注的话题」（带 id，可直接拉流）
- **修复「未知作者」**：搜索结果的结构是 {"info":{…帖子…},"user":{…作者…}}（作者与帖子是兄弟节点），
  宽松解析只在自己身上找作者字段，于是全显示成「未知作者」；现在解析时会把上层节点的作者对象往下继承，
  并补充 user_info / userinfo / author / hb_user 等容器名
- **首页信息流屏蔽视频帖**：发帖过滤组新增「屏蔽视频帖」开关（默认关）。判定不依赖混淆名：
  首页流模型走宿主 `LinkFeedsFlowItemModel.hasVideo()`（即 `getLinkStyle() == LinkStyle.VIDEO_LINK`，
  方法改名时回退比较 LinkStyle 名字），旧渲染链（`BBSLinkObj`）走 `has_video` / `video_url` / `video_info`。
  挂点覆盖模块既有的四处信息流过滤：流模型反序列化、首页流列表、瀑布流卡片、
  低版本列表适配器与 `b.L` / `b.N` 旧渲染链
- **自动清理失效收藏**：新增「自动清理失效收藏」开关（默认关）。收藏列表装载后若含失效条目
  （宿主 `BBSLinkObj.is_deleted == "1"`，与宿主灰化判定同源），自动发起宿主自带的清理请求——
  直接复用 `BBSKtUtils$Companion` 的确认监听器，由宿主自己调 `bbs/app/profile/fav/folder/clean`
  并处理返回，模块只负责「发现 + 触发」，不重造接口。覆盖「我的收藏 / 收藏 tab」列表页与单收藏夹页，
  清理成功后自动刷新列表；同一页面实例 30 秒内只触发一次，接口失败不反复重试

- **详细日志开关**：新增「详细日志」（**默认关**）。关闭时模块日志文件**只写错误日志**，
  屏蔽类日志只保留链路与判定原因、不写帖子标题/作者/等级；开启后记录全部级别并附带帖子信息。
  「记录日志」为文件日志总开关，两者需同时开启才会落盘
- **面板内日志预览**：新增「查看日志」，直接在设置面板里预览最近 200 行模块日志（显示当前日志大小），
  支持「复制最近日志」（最多 2000 行）与「清空」，无需先导出文件
- **清除日志**：设置页「通用与备份 → 通用」新增「清除日志」，一键删除模块日志文件
  （`log.txt` / `log.1.txt`）与运行检查点，弹窗二次确认后执行，与「导出日志」配套

- **调试开关：忽略版本降级限制**：模块会用「版本下限」记录见过的最高版本码，
  装回更旧的模块时拒绝激活（Debug 构建本就绕过）。新增设置项
  「通用与备份 → 通用 → 调试：忽略版本降级限制」：打开即把版本下限清零，
  之后可以随意装回旧版本；关掉后下次启动重新写入下限，限制照常生效

### 修复

- **首页推广屏蔽不生效（#31）**：`屏蔽推广贴` 原只挂在 `bbs.utils.b` 的 `L` / `N` 上，
  那两条是 `BBSLinkObj` 列表（社区 / 搜索 / 新闻 / 频道 / 话题详情）的渲染链；
  首页推荐流自 1.3.39x 起走独立的 Epoxy 链（`RecommendFeedsFlowFragment` →
  `RecommendFlowRVController.buildModels` ← `FeedsFlowItemModelDeserializer`），
  卡片模型是 `IFeedsFlowItemModel` 而不是 `BBSLinkObj`，永远不经过 `L` / `N`，所以开关对首页零覆盖。
  现在判据统一收敛到 `PromoteDetector`，并在首页流的**数据层、列表层、渲染层**三处同时生效
- **推广 / 广告判据全面重做**：不再只认 `content_type` 28 / 29
  - 广告投放上报数据非空（`adReport` / `ad_report`，字段为 `idea_id`、`unit_id`、`ads_id`、
    `ad_pm` 曝光、`ad_cm` 点击）——宿主自己就是靠它发曝光与点击
  - 宿主自己的推广与广告判据函数（`bbs.utils.b.w` / `z`，由 DexKit 定位，宿主调整口径时自动跟进）
  - `BBSLinkObj` 常量表里名字带 `AD` 的 `CONTENT_TYPE_*`（反射生成，不写死 23 / 26 / 27 / 28 / 29）
  - 首页流插入式广告卡 `AdFeedsFlowItemModel`、`label` 为 `advertise`、官方推广号昵称
  覆盖面从「社区推广贴」扩到：首页推广卡、首页广告横幅（新增 `FeedBannerHook` 屏蔽 `adBannerList`）、
  信息流推广卡与广告位、社区 / 搜索 / 新闻 / 频道 / 话题推广贴
- **新增 DexKit 目标解析（全版本覆盖）**：新增 `HeyboxTargets`——目标登记表 + 三级取数
  （版本缓存 → 硬编码候选 → DexKit 锚点查询）+ 异步补挂。锚点全部与混淆名无关（例如「参数含
  `BBSLinkObj` 且方法体用『推广』字面量」定位渲染链，「方法体用 `advertise`」定位广告横幅与信息流绑定），
  解析结果按宿主 versionCode 缓存、读回时做签名校验；DexKit 只在候选全落空时后台跑，不拖慢冷启动。
  设置页「通用与备份 → 目标解析状态」可查看每个目标的命中来源与结果，另有
  「诊断：记录首页流条目」开关把每条首页流的判定信息写进日志
- **旧链渲染方法补齐**：除 `bbs.utils.b.L` / `N` 外，`BBSKtUtils.o(...)`（1.3.394+ 的另一条渲染链，
  同样带「推广」分支）此前完全没被 hook，现已一并覆盖；参数位置也不再写死
  （原来把 ViewHolder 硬编码在 `L` 的第 3 参、`N` 的第 0 参），改为按类型扫描参数
- **真机实测（小黑盒 1.3.395 / Android 16）发现并修掉的问题**：
  - **HTTP 客户端捕获方式**：小黑盒的 R8 已把 OkHttp 公开类全部改名
    （`OkHttpClient` → `okhttp3.z`、`Request` → `okhttp3.a0`，`okhttp3.Request` 这个名字根本不存在），
    按类名 hook 直接 ClassNotFoundException。改为**按结构识别**：hook
    `okhttp3.internal.connection.RealCall` 构造函数（R8 改不了参数顺序，第一个参数就是客户端），
    再按方法签名形状找 newBuilder/url/build 与 newCall/execute/code/body/string —— 与名字完全无关
  - **状态写入静默失效**：`App.writeString/readString` 走的是框架 RemotePreferences，
    在宿主进程里服务未绑定时会静默不写，导致"已提醒集合 / 首轮基线 / 上次检查时间"永远为空
    （同一批帖子每轮都会重推）。改用 `HeyboxPrefs`（宿主进程内 SharedPreferences + commit）
  - **冷启动空转**：打开小黑盒时宿主的第一个网络请求还没发生，客户端尚未捕获，首次检查必然空转。
    现在捕获成功后会自动补跑一次检查
- 动态推送一次性把历史帖全推出去：新增**首轮基线**——第一次检查某个关注对象时只登记不推送，
  之后只推新增；同时把时间窗改为**强制生效**（取不到时间戳的帖子直接跳过、超窗跳过），
  单轮最多提醒 5 条，其余留到下一轮
- 应用内横幅只显示第一条：横幅改为**排队依次显示**（最多排 3 条），并把自动关闭定时器与具体
  View 绑定，避免上一条的定时器误关新的一条；一批多条时用一条汇总横幅（「N 条新动态」）
  代替连续弹窗；主动拉取路径此前完全不弹横幅，已修复
- 新增**一键导入关注列表**：读取小黑盒「我关注的」并去重追加到关注对象（带昵称注释，最多 30 个）
- **每日任务"跳小黑盒下载页"**：任务链接不可用时（例如网页中转链接
  `api.xiaoheihe.cn/heybox/game_statistic/` 会 302）会在 WebView 里兜底跳腾讯应用宝的
  「小黑盒」下载页，表现得像"做第一个分享任务时被弹走"。现在自动化期间拦下应用商店 /
  下载落地页与外部 App scheme（`weixin:` / `mqqapi:` / `market:` 等）的跳转，
  踩到下载页直接放弃该步继续下一步，并加单步 15s 超时，坏链接不再卡住整个任务；
  README 的「链接格式」同步补充游戏详情链接示例（`game/share_game_detail?appid=…`）
  与"别用网页中转 / 统计链接"的说明
- **分享面板误点**：分享面板 Hook 挂在通用弹窗基类 `component.i.show()` 上，此前会在
  **任意弹窗**里按文本找「微信 / QQ」并点击，可能点到登录、分享到电脑、H5 引导等无关按钮；
  现在只在分享面板渠道容器 `rv_share_container` 内查找，容器不存在就完全不点
- **分享渠道兜底**：候选对齐设置项语义（QQ → QQ / QQ空间，微信 → 微信 / 朋友圈），
  面板缺少配置渠道时按可用渠道兜底，渠道列表尚未布局完成时重试，避免整步被跳过；
  同一步的重复完成回调不再把下一步顶掉
- **分享接管加固**：QQ / QQ空间 / 微信 / 微博 四个 UMeng handler 的 `share()` 入口、
  `ShareAction.share()` 出口与 `Tencent.shareToQQ` / `shareToQzone` 在自动化期间统一
  伪造成功回调，不会真的把内容发出去，也不会落进 handler 的「未安装 → 跳下载页」分支

### 变更

- **打开「重定向外部链接」时自动关闭「包含小黑盒域名」**：小黑盒域名的官方网页 / 小程序
  （如「赛事中心」= 小程序 24，页面 `web.xiaoheihe.cn/tools/match_center/home`）依赖内置
  WebView 注入登录态；两个开关同时打开会把这类页面也转外部，外部浏览器拿不到 Cookie
  （不显示盒币等登录信息），页面被拦下后宿主容器又不关闭，回到小黑盒只剩空白页（#33）。
  现在主开关每次打开都把从开关复位为关（回到“仅重定向非小黑盒外链”），确实需要时再单独开启
- **开关联动防链式回写**：联动写入期间抑制 `setChecked` 回调，避免「打开从开关自动开主开关」
  被「打开主开关复位从开关」当场改回去

- **屏蔽日志更详细**：所有屏蔽 / 过滤日志统一成
  `屏蔽内容[链路] 原因=命中规则 | 标题=…, 作者=…, 等级=…, ct=…`，链路标明来源
  （`首页流` / `首页流列表` / `首页卡片` / `列表绑定` / `旧 BBS 列表`），原因写清具体判据：
  `adReport 非空（投放上报数据）`、`content_type=28 属于宿主广告常量表`、
  `content_type=28 命中宿主判据 w()（社区推广）`、`label=advertise`、`推广号作者 X`、
  `等级 Lv2 < 阈值 Lv5`、`无等级数据 < 阈值 Lv5`、`视频帖（link_style=VIDEO_LINK）`、
  `命中关键词 白嫖` / `命中关键词 regex:…`。广告类日志同时补上目标方法、目标来源
  （缓存 / 候选 / DexKit）以及条目 `title`、`author`、`link_id`、`ads_id`

- 设置面板里过长的说明文字统一改成一句话短句（分类入口只留关键词摘要），行内更易扫读
- 跨进程开关广播（`PreferenceReceiver`）改为运行时解析模块包名，不再写死 `com.better.heybox`
- 项目改用 **GPL-3.0-or-later** 授权：新增根目录 `LICENSE`（GPLv3 完整原文逐字未改 + 依第 7 条作出的项目附加条款：署名与非关联声明保留 / 修改版本标注 / 名称与商标限制 / 免责与责任限制 / **宿主应用例外** / 第三方组件许可），README 增加「开源协议」一节
- README 的「免责声明」改为「免责声明与使用倡议」：商业用途、24 小时删除、平台内传播等表述调整为作者立场与风险提示，并注明不构成 GPLv3 之外的附加许可条件

## 0.7.7

### 变更

- **液态玻璃底栏开关即时生效**：运行时安装/卸载玻璃条并把宿主原始底栏装回原位
  （含选中监听、加号浮钮、内容区布局规则、渐变阴影的完整还原），不再需要重启小黑盒；
  设置面板与玻璃调节面板的「液态玻璃底栏」开关同步去掉重启提示
- 沉浸式小白条独立于液态玻璃开关：关闭玻璃后开关同样作用于经典底栏
  （加高 rg_main 让底栏背景延伸进手势区，tab 内容与加号浮钮自动避开小白条）

### 修复

- 关闭液态玻璃后「隐藏加号」不生效（#24）：经典底栏路径此前只隐藏了加号槽位占位视图，
  真正的加号浮钮由液态玻璃安装器接管隐藏，玻璃关闭时没有任何代码隐藏它，现已恢复
- 「沉浸式小白条」开关无效（#25）：窗口级沉浸实现（导航栏透明、去除对比度遮罩、
  清除导航栏 inset）自引入起从未被调用；玻璃条手势区避让也与开关脱钩
  （Android 13+ 恒延伸、旧版本恒避让）。现安装/恢复时重断言窗口沉浸，
  手势区避让统一由开关控制，两个设置入口切换均即时生效

## 0.7.5

### 变更

- **适配小黑盒 1.3.395**：
  - 版本前置检测与受支持列表加入 `1.3.395`
  - 设置页入口方法适配新混淆名：1.3.395 把 1.3.394 的 `L1` 改名为 `N1`
    （两者字节码结构一致，仅 Robust 方法索引不同），候选改为按版本倒序尝试
  - 气泡广告 / 角标广告拉取入口适配 `module.ads.h` 的成段重命名：
    角标拉取 `h(x0$g)` → `i(x0$g)`，气泡展示 `l(h$g)` → `s(h$i)`
    （方法名与回调内部类同时改名；注意 395 中 `h$g` 仍存在但已是角标相关的新类型）
  - 上述 Hook 目标改为**跨版本候选列表**，新版再次改名时优先自动命中，无需改动代码结构

### 修复

- 气泡广告 / 角标广告 Hook 找不到目标方法时不再静默失败：
  此前 `catch (NoSuchMethodException ignored) {}` 会让过滤功能悄悄失效且不留任何日志，
  现改为候选全部落空时输出 WARN 提示

## 0.7.2

### 新增

- **首次启动免责声明**：首次打开小黑盒强制弹出免责声明，需「同意并继续」方可进入；
  「不同意并退出」直接退出应用，同意后不再重复显示
- **关于分组**：设置面板新增「关于」分组，可查看模块版本号并一键打开 GitHub 仓库

### 变更

- Release 构建保留 WARN 级日志，便于不带「记录日志」开关排查问题

### 修复

- 深色模式下液态玻璃底栏反色状态卡住，玻璃条颜色不随主题切换（#19）
- 底栏隐藏项按槽位定位（#21）：服务端动态下发 tab 文案时隐藏目标不再错位，
  设置面板同步显示运行时真实标签
- 多行编辑弹窗内容过长时不可滚动、列表过滤崩溃（#22）：弹窗改为可滚动，关键词过滤容错
- 「屏蔽无等级用户」误伤系统账号内容导致热点信息流加载异常（#23）：现仅对真实用户发帖生效
- AI 标题党判定结果立即生效（此前需重进信息流）；新增「输出 Token 上限」设置，
  避免判定 JSON 被截断失效

## 0.7.0

### 新增

- **发帖过滤**（#15）：
  - **屏蔽低等级发帖**：按帖子自带等级数据过滤低等级用户，阈值关闭 / Lv1-Lv10 可选，
    可选屏蔽无等级徽章账号
  - **关键词屏蔽**：命中标题或正文即屏蔽，一行一个，`regex:` 前缀支持正则
  - **AI 标题党识别**（默认关闭）：OpenAI 兼容接口逐条判定标题党并屏蔽，
    内置 DeepSeek / Kimi / 通义 / 智谱 / OpenAI / OpenRouter / 本地模型预设，判定提示词可自定义、支持一键测试连接；
    帖子标题会发送到所配置的 AI 服务商，请知悉
  - 覆盖首页推荐 / 热点 / 话题详情等信息流列表，本地缓存内容在重新绑定时补拦；
    新增配置纳入配置备份
- **浏览器重定向**（默认关闭）：内置网页中的链接按规则改由系统浏览器打开，
  启动 / 页面加载 / 页内跳转 / 容器兜底四层拦截，基本消除跳转白屏与闪页；
  默认仅重定向非小黑盒域名的外链，登录 / 授权 / 支付等敏感页与 `.apk` 下载强制留内置；
  「包含小黑盒域名」开启后已知域名也重定向（敏感页仍强制内置）；
  支持「强制重定向域名」「强制内置域名」名单；
  与「重定向浏览器」指定包名；
  相关配置纳入配置备份
- **网页日志**：记录内置浏览器打开的页面与标题（最近 80 条），
  设置面板可查看、全部复制、清空
- **每日任务完成后返回首页**（#13）：自动化分享全部完成后自动退回小黑盒首页，清理途中打开的帖子页；设置面板「每日任务」新增开关（默认开启），并纳入配置备份
- **液态玻璃同步上游 v1.5.0**：
  - **玻璃条宽度三模式**（自适应/占满/自定义百分比），默认自适应——按标签数量取合适宽度并居中，宽屏上玻璃条不再横贯全屏
  - **底栏形态系统**（经典居中/右侧圆钮/自动），右侧圆钮形态用圆形玻璃按钮承载加号
  - **Tab 宽度调节**（50-150%）与宽度变化过渡动画：隐藏/显示标签、调节宽度时标签与水滴同拍滑动，加号随中缝平滑跟随
  - **加号点击弹跳**：点击加号获得与选中 tab 图标同款的弹跳反馈
  - **底部提示条抬升避让**：底部 Toast、应用内通知（更新/推荐横幅）自动抬到玻璃栏上方 12dp，不再被玻璃条遮挡
  - 重复点击当前 tab 刷新：不再限定标题白名单，任何 tab 的重复点击都按宿主语义刷新
- **玻璃条宽度模式 / 自定义宽度 / Tab 宽度 / 底栏形态**纳入配置备份

### 变更

- 设置面板分组调整：网页相关设置（重定向 / 网页 DevTools / 打开网页 / 网页日志）
  归入「网页」分组；新增开关联动——「重定向外部链接」与「网页 DevTools」互斥，「包含小黑盒域名」跟随重定向主开关，联动原地刷新不跳回顶部
- 「玻璃宽度自适应」更名为「加长选中 Tab」，生效条件与上游对齐

### 修复

- 原生弹窗（分享渠道 / 链接编辑 / 导入确认 / 保存位置）误回退系统样式：隐形探针分类时
  未排除自身注入的替换视图，导致 Builder 方法分类失败；现剔除替换视图并自动重跑探针
  （最多三轮）（#17）

## 0.6.0

### 新增

- **液态玻璃底栏**：底部导航栏渲染为实时折射/色散的「液态玻璃」效果，带玻璃水滴选中动画；Android 13+ 启用 GPU 渲染，低版本自动回退毛玻璃。长按首页标题栏图标或设置页「通用设置」行打开调节面板，可调深/浅底色、不透明度、高度、偏移、沉浸式小白条、自适应反色、宽度自适应
- **液态玻璃实现选择**：与独立的「小黑盒液态玻璃」模块共存时，首次打开小黑盒自动弹出选择，可让玻璃效果由该模块提供（本模块的玻璃底栏与长按入口同步让位，避免双重底栏）；提供方选择纳入配置备份
- **屏蔽双列信息流（实验性）**：将首页推荐 / 话题 / 百科的双列瀑布流恢复为单列，封面比例同步修正（仅 1.3.394 生效，其他版本不显示开关）
- **网页 DevTools**：为小黑盒内置 WebView 开启 Chrome 远程调试
- **打开网页**：设置页输入网址后用小黑盒内置浏览器打开

### 变更

- 底部导航栏隐藏优化：剩余 tab 自动等分、选中项被隐藏时自动切换，与液态玻璃联动热生效
- 视频下载悬浮按钮与液态玻璃调节面板互不遮挡；液态玻璃设置纳入配置备份
- 设置面板分组调整：「底部导航栏隐藏」移至面板顶部
- 工程调整：第三轮复杂化清理（约 300 行）——删除死代码与重复实现，弹窗 / 选中监听 / Intent 构造等复制粘贴样板收敛，玻璃加号居中定位收敛为布局监听单机制，DexKit 缓存改用标准 Properties 存储，下载任务 headers 持久化改 JSON 嵌套（兼容旧格式）

### 修复

- 屏蔽社区推广贴失效：社区/搜索/新闻/频道列表实际渲染走 `bbs.utils.b` 的 N 方法（M 及各绑定器只是转调入口），此前只拦截了话题详情/概念链专用的 L 方法，两处现已同时拦截；顺带修复被隐藏的卡片复用给正常帖子时连带消失的问题
- 修复关闭「自适应反色」时浅色模式被误判为深色（玻璃标签色昼夜判断错误）
- 修复视频下载悬浮按钮监听器泄漏：反复进出视频页会在进程内累积持有已销毁页面的视图树
- 修复配置备份遗漏「液态玻璃提供方」选择的问题

## 0.5.0

### 新增

- **设置页自动分析（DexKit）**：小黑盒更新打乱混淆名后自动重新定位设置目标；原生弹窗（分享渠道 / 链接编辑 / 导入确认 / 保存位置）用隐形探针按实际渲染位置自动分类，结果按宿主版本缓存，解析失败自动回退系统弹窗

### 变更

- APK 体积优化：release 启用 R8 + 资源裁剪，剔除 x86 库
- 设置面板开关支持标题下方灰色小字介绍；「保存位置」确认弹窗改用小黑盒原生弹窗

## 0.4.5

### 新增

- **净化分享链接**：复制 / 分享链接时自动去掉小黑盒链接上的追踪参数，保留功能参数，链接照常打开
- **小黑盒视频下载**：视频帖悬浮下载按钮 + 底部下载面板；支持 mp4 直链与 HLS 分片流、断点续传、自动转封装 MP4、帖子标题命名、SAF 保存位置、通知栏反馈

### 变更

- 正式版（Release）只保留 error 级日志

### 修复

- 修复单参数重载 Hook 参数越界、通知被系统拒收、暂停/继续竞态、HLS 合并文件误删等问题

## 0.4.1（未公开）

### 新增

- Debug 版运行检查点：模块生命周期与各功能 Hook 安装节点打点，设置页可跨进程查看
- 导出模块日志：运行状态 + 检查点 + 日志文件一键导出

### 工程调整

- CI 构建 Debug APK 便于排查；Release 工作流保持正式构建

## 0.4.0

### 新增

- **配置导入 / 导出**：模块设置页与小黑盒内嵌面板共用同一 JSON 格式，覆盖全部开关与字符串配置

### 工程与依赖升级

- Gradle / AGP / CI Action 全面升级

## 0.3.5

### 修复

- 修复 394 设置 UI 行高 3 倍问题（硬编码资源 ID 改为运行时解析）

### 新增

- **每日任务自动化**：自动完成 3 种分享任务，链接独立配置、渠道可选、跨天重置
- 伪装通知权限：让小黑盒认为通知已开启，获得签到加成
- 修复图片系统分享（优先保存系统相册）、长文正文文字短暂消失、原生弹窗按钮不关闭等问题

## 0.3.2

### 双版本兼容

- 支持小黑盒 1.3.393 / 1.3.394 双版本，混淆目标自动回退

## 0.3.1

### 工程重构

- MainModule 精简为入口，各功能 Hook 按模块拆分到 `hooks/` 子包
- 新增「记录日志」开关与文件日志
- 小黑盒内设置改为本进程直读直写，不再跨进程

## 0.2.1

### 内嵌设置与主题

- 小黑盒设置页直接渲染模块面板，支持深色模式三态与 Monet 动态取色

## 0.2.0

首个可分发版本：广告过滤、底部导航栏屏蔽、解除复制、文本选择修复。
