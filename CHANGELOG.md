# Changelog

本项目版本号采用 `v主版本.次版本.修订版本` 格式。推送代码时，CI 会构建带日期和当日序号的版本并将 APK 保存在 Action 成品中；Release 工作流手动输入版本号后创建正式 Release，并在说明中列出 commit 更新内容。

## 0.7.0

### 新增

- **发帖过滤**（#15）：
  - **屏蔽低等级发帖**：按帖子自带等级数据过滤低等级用户，阈值关闭 / Lv1-Lv10 可选，
    可选屏蔽无等级徽章账号（含官方号，默认放行）
  - **关键词屏蔽**：命中标题或正文即屏蔽，一行一个，`regex:` 前缀支持正则
  - **AI 标题党识别**（默认关闭）：OpenAI 兼容接口逐条判定标题党并屏蔽，
    内置 DeepSeek / Kimi / 通义 / 智谱 / OpenAI / OpenRouter / 本地模型预设
    （选中自动填充地址与模型），判定提示词可自定义、支持一键测试连接；
    帖子标题会发送到所配置的 AI 服务商，请知悉
  - 覆盖首页推荐 / 热点 / 话题详情等信息流列表，本地缓存内容在重新绑定时补拦；
    新增配置纳入配置备份（AI Token 除外）
- **每日任务完成后返回首页**（#13）：自动化分享全部完成后自动退回小黑盒首页，清理途中打开的帖子页；设置面板「每日任务」新增开关（默认开启），并纳入配置备份
- **液态玻璃同步上游 v1.5.0**：
  - **玻璃条宽度三模式**（自适应/占满/自定义百分比），默认自适应——按标签数量取合适宽度并居中，宽屏上玻璃条不再横贯全屏
  - **底栏形态系统**（经典居中/右侧圆钮/自动），右侧圆钮形态用圆形玻璃按钮承载加号
  - **Tab 宽度调节**（50-150%）与宽度变化过渡动画：隐藏/显示标签、调节宽度时标签与水滴同拍滑动，加号随中缝平滑跟随
  - **加号点击弹跳**：点击加号获得与选中 tab 图标同款的弹跳反馈
  - **底部提示条抬升避让**：底部 Toast、应用内通知（更新/推荐横幅）自动抬到玻璃栏上方 12dp，不再被玻璃条遮挡
  - 重复点击当前 tab 刷新：不再限定标题白名单，任何 tab 的重复点击都按宿主语义刷新
- **玻璃条宽度模式 / 自定义宽度 / Tab 宽度 / 底栏形态**纳入配置备份

### 变更

- 「玻璃宽度自适应」更名为「加长选中 Tab」，生效条件与上游对齐（有标签被隐藏时选中项加长）

### 修复

- 原生弹窗（分享渠道 / 链接编辑 / 导入确认 / 保存位置）误回退系统样式：隐形探针分类时
  未排除自身注入的替换视图，导致 Builder 方法分类失败；现剔除替换视图并自动重跑探针
  （最多三轮）（#17）

## 0.6.0

### 新增

- **液态玻璃底栏**：底部导航栏渲染为实时折射/色散的「液态玻璃」效果，带玻璃水滴选中动画；Android 13+ 启用 GPU 渲染，低版本自动回退毛玻璃。长按首页标题栏图标或设置页「通用设置」行打开调节面板，可调深/浅底色、不透明度、高度、偏移、沉浸式小白条、自适应反色、宽度自适应
- **液态玻璃实现选择**：与独立的「小黑盒液态玻璃」模块共存时，首次打开小黑盒自动弹出选择，可让玻璃效果由该模块提供（本模块的玻璃底栏与长按入口同步让位，避免双重底栏）；提供方选择纳入配置备份
- **屏蔽双列信息流（实验性）**：将首页推荐 / 话题 / 百科的双列瀑布流恢复为单列，封面比例同步修正（仅 1.3.394 生效，其他版本不显示开关）
- **网页 DevTools**：为小黑盒内置 WebView 开启 Chrome 远程调试
- **打开网页**：设置页输入网址后用小黑盒内置浏览器打开

### 变更

- 底部导航栏隐藏优化：剩余 tab 自动等分、选中项被隐藏时自动切换，与液态玻璃联动热生效
- 视频下载悬浮按钮与液态玻璃调节面板互不遮挡；液态玻璃设置纳入配置备份
- 设置面板分组调整：「底部导航栏隐藏」移至面板顶部
- 工程调整：第三轮复杂化清理（约 300 行）——删除死代码与重复实现，弹窗 / 选中监听 / Intent 构造等复制粘贴样板收敛，玻璃加号居中定位收敛为布局监听单机制，DexKit 缓存改用标准 Properties 存储，下载任务 headers 持久化改 JSON 嵌套（兼容旧格式）

### 修复

- 屏蔽社区推广贴失效：社区/搜索/新闻/频道列表实际渲染走 `bbs.utils.b` 的 N 方法（M 及各绑定器只是转调入口），此前只拦截了话题详情/概念链专用的 L 方法，两处现已同时拦截；顺带修复被隐藏的卡片复用给正常帖子时连带消失的问题
- 修复关闭「自适应反色」时浅色模式被误判为深色（玻璃标签色昼夜判断错误）
- 修复视频下载悬浮按钮监听器泄漏：反复进出视频页会在进程内累积持有已销毁页面的视图树
- 修复配置备份遗漏「液态玻璃提供方」选择的问题

## 0.5.0

### 新增

- **设置页自动分析（DexKit）**：小黑盒更新打乱混淆名后自动重新定位设置目标；原生弹窗（分享渠道 / 链接编辑 / 导入确认 / 保存位置）用隐形探针按实际渲染位置自动分类，结果按宿主版本缓存，解析失败自动回退系统弹窗

### 变更

- APK 体积优化：release 启用 R8 + 资源裁剪，剔除 x86 库
- 设置面板开关支持标题下方灰色小字介绍；「保存位置」确认弹窗改用小黑盒原生弹窗

## 0.4.5

### 新增

- **净化分享链接**：复制 / 分享链接时自动去掉小黑盒链接上的追踪参数，保留功能参数，链接照常打开
- **小黑盒视频下载**：视频帖悬浮下载按钮 + 底部下载面板；支持 mp4 直链与 HLS 分片流、断点续传、自动转封装 MP4、帖子标题命名、SAF 保存位置、通知栏反馈

### 变更

- 正式版（Release）只保留 error 级日志

### 修复

- 修复单参数重载 Hook 参数越界、通知被系统拒收、暂停/继续竞态、HLS 合并文件误删等问题

## 0.4.1（未公开）

### 新增

- Debug 版运行检查点：模块生命周期与各功能 Hook 安装节点打点，设置页可跨进程查看
- 导出模块日志：运行状态 + 检查点 + 日志文件一键导出

### 工程调整

- CI 构建 Debug APK 便于排查；Release 工作流保持正式构建

## 0.4.0

### 新增

- **配置导入 / 导出**：模块设置页与小黑盒内嵌面板共用同一 JSON 格式，覆盖全部开关与字符串配置

### 工程与依赖升级

- Gradle / AGP / CI Action 全面升级

## 0.3.5

### 修复

- 修复 394 设置 UI 行高 3 倍问题（硬编码资源 ID 改为运行时解析）

### 新增

- **每日任务自动化**：自动完成 3 种分享任务，链接独立配置、渠道可选、跨天重置
- 伪装通知权限：让小黑盒认为通知已开启，获得签到加成
- 修复图片系统分享（优先保存系统相册）、长文正文文字短暂消失、原生弹窗按钮不关闭等问题

## 0.3.2

### 双版本兼容

- 支持小黑盒 1.3.393 / 1.3.394 双版本，混淆目标自动回退

## 0.3.1

### 工程重构

- MainModule 精简为入口，各功能 Hook 按模块拆分到 `hooks/` 子包
- 新增「记录日志」开关与文件日志
- 小黑盒内设置改为本进程直读直写，不再跨进程

## 0.2.1

### 内嵌设置与主题

- 小黑盒设置页直接渲染模块面板，支持深色模式三态与 Monet 动态取色

## 0.2.0

首个可分发版本：广告过滤、底部导航栏屏蔽、解除复制、文本选择修复。
