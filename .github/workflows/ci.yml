name: CI

on:
  push:
    branches:
      - "**"

# Serialize CI builds so the daily build number is deterministic.
concurrency:
  group: ci-daily-build-number
  cancel-in-progress: false

permissions:
  contents: read
  actions: read

jobs:
  build:
    name: Build CI APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: 21

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Calculate CI version
        id: version
        env:
          GH_TOKEN: ${{ github.token }}
          GITHUB_RUN_ID: ${{ github.run_id }}
        run: |
          base_version="$(git tag --list 'v*' --sort=-version:refname | head -n 1)"
          if [ -z "$base_version" ]; then
            base_version="$(sed -n 's/^VERSION_NAME=//p' gradle.properties | head -n 1)"
          fi
          base_version="${base_version#v}"
          if [[ ! "$base_version" =~ ^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)$ ]]; then
            echo "VERSION_NAME must be numeric semver (x.y.z), got: $base_version" >&2
            exit 1
          fi

          build_date="$(date -u +%y%m%d)"
          build_date_iso="$(date -u +%Y-%m-%d)"
          runs_today="$(gh api --paginate \
            "/repos/${GITHUB_REPOSITORY}/actions/workflows/ci.yml/runs?event=push&created=${build_date_iso}..${build_date_iso}&per_page=100" \
            --jq '.workflow_runs[].id' 2>/dev/null \
            | grep -v "^${GITHUB_RUN_ID}$" \
            | sort -u \
            | wc -l \
            | tr -d ' ')"
          daily_build=$(( ${runs_today:-0} + 1 ))
          ci_version="v${base_version}-${build_date}${daily_build}"

          echo "version=${ci_version}" >> "$GITHUB_OUTPUT"
          echo "version_name=${ci_version}" >> "$GITHUB_OUTPUT"
          echo "version_code=${GITHUB_RUN_NUMBER}" >> "$GITHUB_OUTPUT"
          echo "CI version: ${ci_version}"

      - name: Build debug APK
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug \
            --project-prop VERSION_NAME="${{ steps.version.outputs.version_name }}" \
            --project-prop VERSION_CODE="${{ steps.version.outputs.version_code }}"

      - name: Prepare CI artifact
        env:
          CI_VERSION: ${{ steps.version.outputs.version }}
        run: |
          mkdir -p dist
          cp app/build/outputs/apk/debug/*.apk "dist/BetterHeybox-${CI_VERSION}.apk"
          printf '%s\n' "$CI_VERSION" > dist/version.txt

      - name: Upload CI artifact
        uses: actions/upload-artifact@v7
        with:
          name: BetterHeybox-${{ steps.version.outputs.version }}
          path: dist/
          retention-days: 14

      - name: Build Telegram message
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          CI_VERSION: ${{ steps.version.outputs.version }}
          BEFORE_SHA: ${{ github.event.before }}
          TRIGGER_ACTOR: ${{ github.actor }}
        run: |
          set -euo pipefail

          escape_html() {
            sed -e 's/&/\&amp;/g' -e 's/</\&lt;/g' -e 's/>/\&gt;/g'
          }

          repo_url="https://github.com/${GITHUB_REPOSITORY}"
          run_url="${repo_url}/actions/runs/${GITHUB_RUN_ID}"
          full_sha="$(git rev-parse HEAD)"
          short_sha="$(git rev-parse --short=7 HEAD)"
          subject="$(git log -1 --pretty=%s | escape_html)"
          actor="$(printf '%s' "$TRIGGER_ACTOR" | escape_html)"
          branch="$(printf '%s' "${GITHUB_REF_NAME}" | escape_html)"

          apk="dist/BetterHeybox-${CI_VERSION}.apk"
          size_bytes="$(wc -c < "$apk" | tr -d '[:space:]')"
          size_human="$(awk -v b="$size_bytes" 'BEGIN { printf "%.2f MB", b / 1048576 }')"
          sha256="$(sha256sum "$apk" | cut -d' ' -f1)"

          # before 为全零说明是新分支 / 强推，没有可对比的基线
          compare_line=""
          if [ -n "$BEFORE_SHA" ] && ! printf '%s' "$BEFORE_SHA" | grep -qE '^0+$'; then
            compare_line="$(printf '对比: <a href="%s/compare/%s...%s">%s...%s</a>' \
              "$repo_url" "$BEFORE_SHA" "$full_sha" "${BEFORE_SHA:0:7}" "$short_sha")"
          fi

          # 只发一条消息：这段正文就是 APK 的 caption（上限 1024），
          # 所以文件 / 校验 / 运行链接这类关键信息放前面，被截断时先丢末尾的解释性内容
          {
            printf '🔧 <b>CI 构建成功</b> · <code>%s</code>\n' "$CI_VERSION"
            printf '文件: %s（%s）\n' "$(basename "$apk")" "$size_human"
            printf 'SHA-256: <code>%s</code>\n' "$sha256"
            printf '运行: <a href="%s">#%s</a>（Actions 产物保留 14 天）\n' "$run_url" "${GITHUB_RUN_NUMBER}"
            printf '\n分支: <code>%s</code>\n' "$branch"
            printf '提交: <a href="%s/commit/%s">%s</a> %s\n' "$repo_url" "$full_sha" "$short_sha" "$subject"
            if [ -n "$compare_line" ]; then
              printf '%s\n' "$compare_line"
            fi
            printf '作者: %s\n' "$actor"
          } > telegram-message.html

      - name: Notify Telegram
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        continue-on-error: true
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CI_CHAT_ID }}
          TELEGRAM_MESSAGE_THREAD_ID: ${{ secrets.TELEGRAM_MESSAGE_THREAD_ID }}
          TELEGRAM_SILENT: ${{ vars.TELEGRAM_CI_SILENT }}
        run: |
          bash .github/scripts/telegram-notify.sh \
            --message telegram-message.html \
            --document "dist/BetterHeybox-${{ steps.version.outputs.version }}.apk"
